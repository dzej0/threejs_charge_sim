{
  "dependencies": {
    "gh-pages": "^6.3.0",
    "three": "^0.169.0",
    "three-trackballcontrols": "^0.9.0"
  },
  "scripts": {
    "deploy": "gh-pages -d dist",
    "build": "npx vite build"
  }
}
